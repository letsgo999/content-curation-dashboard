{
  "name": "content-curation-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=bundle.js --jsx=automatic --platform=browser --target=es2020 --log-level=warning"
  },
  "dependencies": {
    "@google/genai": "^1.15.0",
    "cheerio": "^1.0.0-rc.12",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
